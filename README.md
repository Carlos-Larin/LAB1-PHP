CREATE TABLE `registro`.`datos1` (`Nombre` VARCHAR(200) NOT NULL , `Apellido` VARCHAR(200) NOT NULL , `Telefono` VARCHAR(200) NOT NULL , `FechaNac` DATE NOT NULL ) ENGINE = InnoDB;

esquema de la Database
